import React, {ReactElement} from 'react';
import {Text, View} from 'react-native';

const CompletedScreen = (): ReactElement => {
  return (
    <View>
      <Text>Completed Screen</Text>
    </View>
  );
};

export default CompletedScreen;
