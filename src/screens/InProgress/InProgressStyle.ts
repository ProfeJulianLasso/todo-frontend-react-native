import {StyleSheet} from 'react-native';

export const inProgressStyle = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 16,
  },
  center: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
