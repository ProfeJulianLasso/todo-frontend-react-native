import React, {ReactElement} from 'react';
import {Text, View} from 'react-native';

const InProgressScreen = (): ReactElement => {
  return (
    <View>
      <Text>InProgress Screen</Text>
    </View>
  );
};

export default InProgressScreen;
